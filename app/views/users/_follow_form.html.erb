<% unless current_user?(@user) %>
  <div id="follow_form" class="btn-group btn-group-sm">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>

  </div>
<% end %>
