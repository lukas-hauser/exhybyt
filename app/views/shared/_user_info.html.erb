<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.display_name %></h1>
<span><%= link_to "view profile", current_user %></span>
<span><%= pluralize(current_user.spaces.count, "space") %></span>
<span><%= pluralize(current_user.artworks.count, "artwork") %></span>
